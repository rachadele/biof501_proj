process.executor = 'local'

process {
    cache = 'standard'  // Options: 'standard' (default), 'deep', 'lenient', or 'false'
    container = 'raschwaa/census-pipeline:latest'

}

nextflow {
    clean {
        afterRun = false  // Default is false; set to true to clean up automatically
    }
}


 docker { 
  	enabled = true
    // runOptions = '--mount  -v source=/Users/Rachel/Documents/BIOF501/biof501_proj,target=/biof501_proj'
    runOptions = "-v $projectDir:/biof501_proj$projectDir -m 8g --memory-swap 20g'"

    temp = 'auto'
}	 

//    
